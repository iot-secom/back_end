"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
var PWD_MATCH = "/pwd";
var MAC = "/mac";

var routes = {
  // update: (id, flag) => {
  // 	if (id) {
  // 		console.log(id);
  // 		return `/edit/${id}/${flag}`;
  // 	} else {
  // 		return EDIT_FLAG;
  // 	}
  // },
  pwd_match: PWD_MATCH,
  mac: MAC
};

exports.default = routes;