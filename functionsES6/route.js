const PWD_MATCH = '/pwd';

const routes = {
	// update: (id, flag) => {
	// 	if (id) {
	// 		console.log(id);
	// 		return `/edit/${id}/${flag}`;
	// 	} else {
	// 		return EDIT_FLAG;
	// 	}
	// },
	pwd_match: PWD_MATCH,
};

export default routes;
